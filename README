Time. Boats.

Needs coffeescript and browserify
To compile client:
> browserify src/client.coffee -o public/js/timeboats.js 

Getting started with node:
1. install nodejs and npm
2. run 'npm install'
3. run the server with 'coffee src/app.coffee'